<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:orientation="vertical"
    android:padding="16dp">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:cardBackgroundColor="#000000"
        app:cardCornerRadius="12dp">
        <VideoView
            android:id="@+id/videoView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Media Player Ready"
        android:textColor="#03DAC6"
        android:textSize="18sp"
        android:textStyle="bold" />

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:columnCount="2"
        android:rowCount="3">

        <Button android:id="@+id/btnOpenAudio" android:layout_width="0dp" android:layout_columnWeight="1" android:text="Open Audio" android:backgroundTint="#1E1E1E" android:textColor="#FFFFFF" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnOpenURL" android:layout_width="0dp" android:layout_columnWeight="1" android:text="Open URL" android:backgroundTint="#1E1E1E" android:textColor="#FFFFFF" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnPlay" android:layout_width="0dp" android:layout_columnWeight="1" android:text="Play" android:backgroundTint="#03DAC6" android:textColor="#121212" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnPause" android:layout_width="0dp" android:layout_columnWeight="1" android:text="Pause" android:backgroundTint="#BB86FC" android:textColor="#121212" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnStop" android:layout_width="0dp" android:layout_columnWeight="1" android:text="Stop" android:backgroundTint="#CF6679" android:textColor="#FFFFFF" android:layout_margin="4dp"/>
        <Button android:id="@+id/btnRestart" android:layout_width="0dp" android:layout_columnWeight="1" android:text="Restart" android:backgroundTint="#FFD700" android:textColor="#121212" android:layout_margin="4dp"/>
    </GridLayout>

</LinearLayout>